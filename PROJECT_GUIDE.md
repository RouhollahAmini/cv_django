# راهنمای پروژه CV Django

## ساختار پروژه

### 📁 apps/
این پوشه شامل تمام اپلیکیشن های پروژه است:

#### 🔧 apps/core/
- **هدف**: شامل کلاسها و توابع مشترک
- **محتوا**: 
  - `TimeStampedModel`: برای اضافه کردن تاریخ ایجاد و بروزرسانی
  - `BaseModel`: برای فیلدهای مشترک مثل is_active و order
  - `SEOModel`: برای فیلدهای SEO

#### 👤 apps/about/
- **هدف**: صفحه درباره من و اطلاعات شخصی
- **مدل ها**:
  - `Profile`: اطلاعات شخصی
  - `Service`: خدمات ارائه شده
  - `Testimonial`: نظرات مشتریان
  - `Client`: مشتریان

#### 📝 apps/blog/
- **هدف**: وبلاگ و مقالات
- **مدل ها**:
  - `Category`: دسته بندی مقالات
  - `Tag`: تگ های مقالات
  - `Post`: مقالات
  - `Comment`: نظرات مقالات

#### 📞 apps/contact/
- **هدف**: صفحه تماس و فرم تماس
- **مدل ها**:
  - `ContactMessage`: پیام های دریافتی
  - `ContactInfo`: اطلاعات تماس

#### 🎨 apps/portfolio/
- **هدف**: نمونه کارها و پروژه ها (بعداً اضافه می شود)

## مزایای این ساختار

### ✅ ماژولار بودن
- هر بخش جدا از بقیه
- آسان برای نگهداری
- قابلیت توسعه بالا

### ✅ قابل فهم بودن
- نام گذاری واضح
- کامنت های فارسی
- ساختار منطقی

### ✅ قابلیت اسکیل
- اضافه کردن app جدید آسان
- تغییر یک بخش روی بقیه تاثیر نمی گذارد
- قابل استفاده مجدد

## نحوه استفاده

### 1. ایجاد Migration ها
```bash
python manage.py makemigrations
python manage.py migrate
```

### 2. ایجاد Superuser
```bash
python manage.py createsuperuser
```

### 3. اجرای سرور
```bash
python manage.py runserver
```

### 4. پر کردن داده های نمونه
```bash
python manage.py populate_data
```

## URL ها

- `/` - صفحه اصلی (درباره من)
- `/blog/` - وبلاگ
- `/contact/` - تماس با من
- `/admin/` - پنل ادمین

## نکات مهم

### 🔍 Clean Architecture
این پروژه از اصول Clean Architecture استفاده می کند:
- جداسازی منطق کسب و کار
- وابستگی های کم
- قابلیت تست بالا

### 📱 Responsive Design
قالب کاملاً واکنش گرا است و روی همه دستگاه ها کار می کند.

### 🚀 Performance
- استفاده از Cache
- بهینه سازی Query ها
- فشرده سازی فایل ها

### 🔒 Security
- اعتبارسنجی ورودی ها
- محافظت در برابر حملات
- رمزگذاری داده ها

## توسعه بیشتر

برای اضافه کردن بخش جدید:

1. پوشه جدید در `apps/` بسازید
2. فایل های `models.py`, `views.py`, `urls.py` اضافه کنید
3. به `INSTALLED_APPS` اضافه کنید
4. Migration بسازید

مثال:
```python
# apps/portfolio/models.py
from apps.core.models import BaseModel

class Project(BaseModel):
    title = models.CharField(max_length=100)
    description = models.TextField()
    # ...
```